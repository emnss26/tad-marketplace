# test/

Cross-repo end-to-end tests live here. Unit tests live next to the source
files they test (`backend/shared/tokens.test.ts`, `frontend/lib/format.test.ts`).

The big integration test described in CLAUDE.md ("Test de integración
cross-repo") will land here in Sprint 4, driven by `scripts/smoke-e2e.mjs`.
